{% set path = options.link.url|default(path(options.link.route|default(grid.requestConfiguration.getRouteName('create')), options.link.parameters|default([]))) %}

<a href="{{ path }}" class="{{ options.class|default('btn btn-purple btn-lg btn-action-create') }}">
    <i class="{{ action.icon|default('ti-plus') }}"></i> <span>{{ (action.label|trans)|default('toro.ui.create'|trans) }}</span>
</a>
