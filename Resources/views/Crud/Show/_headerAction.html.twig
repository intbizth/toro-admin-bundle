<div class="text-xs-center text-right">
    <a class="btn btn-default btn-lg" href="{{ path(configuration.getRouteName('update'), {id: resource.id}) }}">
        <i class="fa fa-pencil"></i> {{ 'toro.ui.edit'|trans }}
    </a>
    <a class="btn btn-default btn-lg" href="{{ cancel_path|default(app.request.headers.get('referer')) }}">
        {{ 'Back'|trans }}
    </a>
</div>
